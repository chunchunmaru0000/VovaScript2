парсе === (дата){
	с = 0;
	рет = [с];
	для сч в дата:
		если сч == "i":
			++с;
		иначе: если сч == "d":
			--с;
		иначе: если сч == "s":
			с = с с;
		иначе: если сч == "o":
			рет += [с];
	воздать рет[1:];
}

докамест истина:
	парсе(ввод());
	
Ё
diddxosidszidsddoouodoetusinissonosoddo