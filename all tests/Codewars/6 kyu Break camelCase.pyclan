решение === (текст){
	рет = "";
	для с в от 0 до текст.длина() -1 {
		если текст[с].чаркод() больше 64 и текст[с].чаркод() меньше 91:
			рет += " ";
		рет += текст[с];
	}
	воздать рет;
}

пока истина:
	решение(хартия("ввод: "));