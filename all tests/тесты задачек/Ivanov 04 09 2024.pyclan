товары = [];

пока ввод("если выйти напишите [выход]: ") != "выход":
	товары += [[ввод("стоимость: "), ввод("название: "), ввод("вес: "), ввод("количество: ")]];
"";
"---------------------------------------------";
"--------------выбор сохранён-----------------";
"-----начинается выбор изменения товаров------";
"---------------------------------------------";
"";

пока ввод("если выйти напишите [выход]: ") != "выход" {
	операция = ввод("есть операции [\"добавление\", \"изменение цены\", \"удаление\"]: ");
	если !["добавление", "изменение", "удаление"].имеет(операция){
		"ДАННОЙ ОПЦИИ НЕТУ В СПИСКЕ: <" + операция + ">";
		продолжить;
	}
	
	если операция == "добавление" {
		товары += [[ввод("стоимость: "), ввод("название: "), ввод("вес: "), ввод("количество: ")]];
		"---------------------------------------------";
		"-----------товар успешно добавлен------------";
		"---------------------------------------------";
	}
	
	иначели операция == "изменение" { 
		имя = ввод("введите имя товара: ");
		
		индекс_товара = -1;
		для индекс в от 0 до товары.длина() - 1:
			если товары[индекс][1] == имя {
				индекс_товара = индекс;
				выйти;
			}
		если индекс_товара == -1 { 
			"ДАННОГО ТОВАРА НЕТУ В СПИСКЕ: <" + имя + ">";
			продолжить;
		}
		
		"---------------------------------------------";
		"---------сейчас будет ввод изменений---------";
		"---------------------------------------------";
		изменение = [[ввод("стоимость: "), ввод("название: "), ввод("вес: "), ввод("количество: ")]];
		
		где товары[индекс_товара] = изменение;
		"---------------------------------------------";
		"------------товар успешно изменён------------";
		"---------------------------------------------";
	}
	иначели операция == "удаление" {
		имя = ввод("введите имя товара: ");
		
		индекс_товара = -1;
		для индекс в от 0 до товары.длина() - 1:
			если товары[индекс][1] == имя {
				индекс_товара = индекс;
				выйти;
			}
		если индекс_товара == -1 { 
			"ДАННОГО ТОВАРА НЕТУ В СПИСКЕ: <" + имя + ">";
			продолжить;
		}
		
		деяти товары.удали(индекс_товара);
		"---------------------------------------------";
		"------------товар успешно удалён-------------";
		"---------------------------------------------";
	}
}

писать("C:\\Users\\Студент\\Documents\\ВоваСкрипт\\bin\\итог.txt", "линию", товары.перебор(=== эл: эл.соединён(" | ")).соединён(";"));


Ё
товары;